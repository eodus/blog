# Blog redirects

The canonical site moved to <https://eodus.github.io/>.

This repository preserves the former `/blog/` homepage and three published post URLs.
Its `404.html` also redirects other old `/blog/...` paths while preserving query strings and fragments.
